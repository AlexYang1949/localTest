<view class='root'>
  <image class='topImage' src='/source/bindTop.png' ></image>
  <image class='backHome' bindtap='backHome' src='/source/icon_backhome.png'></image>
  <text class='tipsView' wx:if="{{name}}"> 我是{{name}}</text>  
  <text class='tipsView' wx:if="{{name}}"> 我想知道您在哪儿!</text>  
  <form class='form' bindsubmit="bindFormSubmit">
    <textarea class='titleInput' wx:if='{{!title}}' placeholder="请输入邀请人身份" name='textarea'/>
    <button class='bindButton' type="primary" form-type="submit">同意授权</button>
  </form>
</view>